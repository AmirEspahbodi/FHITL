# ============================================================================
# CogniLoop API Configuration
# ============================================================================

# API Base URL
# Set this to your backend API endpoint
# Development: http://localhost:3333/api/v1
# Staging: https://api-staging.cogniloop.com/v1
# Production: https://api.cogniloop.com/v1
VITE_API_BASE_URL=http://localhost:3333/api/v1

# ============================================================================
# Authentication Configuration
# ============================================================================

# Authentication is now MANDATORY for all application access
# The following endpoint must be implemented on your backend:
#
# POST /api/v1/login/access-token
# Request Body: { "username": "string", "password": "string" }
# Response: { "access_token": "string", "token_type": "bearer" }
#
# All protected endpoints must accept Bearer tokens:
# Authorization: Bearer {access_token}

# ============================================================================
# Security Requirements for Production
# ============================================================================

# CRITICAL: The following MUST be configured for production:
#
# 1. HTTPS Only:
#    - All API communication must use HTTPS in production
#    - Update VITE_API_BASE_URL to use https:// protocol
#    - Configure SSL certificates on backend server
#
# 2. CORS Configuration:
#    - Backend must allow frontend domain in CORS settings
#    - Example: Access-Control-Allow-Origin: https://app.cogniloop.com
#    - Do NOT use wildcard (*) in production
#
# 3. Content Security Policy (CSP):
#    - Configure CSP headers to prevent XSS attacks
#    - Restrict script sources to trusted domains
#    - Consult your security team for specific policies
#
# 4. Rate Limiting:
#    - Implement rate limiting on /login endpoint (e.g., 5 attempts per 15 min)
#    - Return 429 status code when rate limit exceeded
#    - Frontend will display appropriate error message
#
# 5. Token Security:
#    - Use strong JWT secrets (256+ bits entropy)
#    - Set appropriate token expiration (e.g., 8 hours)
#    - Implement token refresh mechanism (optional but recommended)
#
# 6. Password Security:
#    - Hash passwords with bcrypt or argon2
#    - Enforce strong password policies
#    - Implement account lockout after failed attempts

# ============================================================================
# Optional Configuration (Future)
# ============================================================================

# Feature Flags
# VITE_ENABLE_UNDO=false
# VITE_ENABLE_REALTIME=false

# Analytics
# VITE_ANALYTICS_ID=

# Debug Mode (disable in production)
# VITE_DEBUG_MODE=false

# ============================================================================
# Environment Setup Instructions
# ============================================================================

# Development:
# 1. Copy this file to .env
# 2. Update VITE_API_BASE_URL to match your local backend
# 3. Ensure backend is running on the configured port
# 4. Run: npm run dev

# Production:
# 1. Set VITE_API_BASE_URL to production API endpoint (HTTPS)
# 2. Ensure all security requirements above are met
# 3. Build: npm run build
# 4. Deploy dist/ folder to your hosting service
# 5. Configure CSP and CORS headers on hosting/CDN

# ============================================================================
